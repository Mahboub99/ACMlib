double polygonArea(vector<double>x, vector<double>y, int n) 
{ 
    double area = 0.0; 
    int j = n - 1; 
    for (int i = 0; i < n; i++) 
    { 
        area += (y[j] + y[i]) * (y[j] - y[i]); 
        j = i;
    } 
    return abs(area / 2.0); 
}
